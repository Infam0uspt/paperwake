#pragma once

// Firmware version, shown on the web admin portal's dashboard and
// /system page, and reported alongside OTA results. Bump on every
// firmware change that's worth identifying in the field.
constexpr const char *kFirmwareVersion = "0.3.0-webadmin";

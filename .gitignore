.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
include/secrets.h
.DS_Store
.claude/

# Source assets (icons, sound files, subtitle text) feed offline
# generation tools (tools/gen_*.py) whose output is already checked in
# under include/ and src/ — not needed to build the firmware itself,
# and includes personal/copyrighted alarm sounds not ours to redistribute.
assets/
